<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WebPaper.LoginHelperWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="WebPaper - Login Helper">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Padding="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Login to Website"
                          FontSize="20"
                          FontWeight="SemiBold"/>
                <TextBlock x:Name="UrlText"
                          Text="Please log in to save your session"
                          FontSize="12"
                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                          Margin="0,4,0,0"/>
            </StackPanel>

            <Button Grid.Column="1"
                    Content="&#xE8BB;"
                    FontFamily="Segoe MDL2 Assets"
                    FontSize="16"
                    Click="CloseButton_Click"
                    Background="Transparent"
                    ToolTipService.ToolTip="Close"/>
        </Grid>

        <!-- WebView2 for login -->
        <Grid Grid.Row="1" Padding="0">
            <WebView2 x:Name="loginWebView"/>

            <!-- Loading indicator -->
            <Grid x:Name="LoadingPanel"
                  Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                  Visibility="Visible">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressRing IsActive="True" Width="50" Height="50"/>
                    <TextBlock Text="Loading login page..."
                              Margin="0,16,0,0"
                              HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Grid>

        <!-- Footer with instructions -->
        <Grid Grid.Row="2"
              Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
              Padding="16"
              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
              BorderThickness="0,1,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="Log in using your credentials" FontWeight="SemiBold"/>
                <TextBlock Text="Your login session will be saved securely for the wallpaper"
                          FontSize="12"
                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <Button Content="Cancel"
                        Click="CancelButton_Click"
                        MinWidth="100"/>
                <Button Content="Save &amp; Close"
                        Click="SaveButton_Click"
                        MinWidth="120"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
